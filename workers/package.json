{
  "name": "agent-api-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers secure API for Agent App — handles Flutterwave calls with server-side secret key",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "logs": "wrangler tail"
  },
  "dependencies": {
    "firebase-auth-cloudflare-workers": "^2.0.2"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  }
}
